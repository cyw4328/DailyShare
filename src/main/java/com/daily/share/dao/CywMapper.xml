<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>

<mapper namespace="com.daily.share.dao.CywDAO">

	<insert id="bigCategoryAdd">
		INSERT INTO mainCategory(main_name,main_admin)VALUES (#{param1},#{param2})
	</insert>
		
	<select id="bigCategoryList" resultType="String">
		SELECT main_name from mainCategory
	</select>
	
	<delete id="bigCategoryDel">
		DELETE FROM mainCategory WHERE main_name = #{param1}
	</delete>
	
	<insert id="middleCategoryAdd">
		INSERT INTO middleCategory(mid_name,mid_admin)VALUES (#{param1},#{param2})
	</insert>
	
	<select id="midCategoyrList" resultType="String">
		SELECT mid_name from middleCategory
	</select>
	
	<delete id="midCategoryDel">
		DELETE FROM middleCategory WHERE mid_name = #{param1}
	</delete>

</mapper>